# Loaded automatically by `npm run dev` (Vite development mode).
# Production builds leave VITE_API_URL unset, so the portal calls the API
# same-origin — i.e. the Express server that serves the built portal.
VITE_API_URL=http://localhost:3000
